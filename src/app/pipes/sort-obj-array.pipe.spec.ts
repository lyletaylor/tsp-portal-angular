import { SortObjArrayPipe } from './sort-obj-array.pipe';

describe('SortObjArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortObjArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
